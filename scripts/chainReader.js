
// const { ethers, waffle, network, upgrades } = require("hardhat");
// const { expect, util } = require("chai");
// const { utils, BigNumber } = require("ethers");
// require("colors");

exports.getBalance = async function (wallet) {
    let balance;
    
    return balance;
};

exports.getPosition = async function (token, wallet) {
    let position;
    
    return balance;
};

exports.getTransactions = async function (token, wallet) {
    let transactions = [];
    
    return transactions;
};

exports.isContract = async function(address) {

    return true;
}
